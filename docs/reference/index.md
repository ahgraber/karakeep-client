# API Reference

Reference pages are generated from the package source and docstrings.

- [Karakeep Client](karakeep.md)
- [Models](models.md)

## Local Build Output

The docs build output is deterministic and written to `site/` (configured by `site_dir` in `mkdocs.yml`).

Build locally with:

```bash
uv run --group docs mkdocs build
```
